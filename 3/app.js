const list = document.querySelector(".subject")
items = document.querySelectorAll(".card") 

function filter() {
    list.addEventListener("click", Event = {
        const_targetid: Event.target.dataset.id
        .console.log(targetId)
    })
}
filter()